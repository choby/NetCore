@model System.Security.Claims.ClaimsPrincipal
@using System.Security.Claims


@(Model.Identity.Name)
<dl>
    @foreach (var claim in (Model as ClaimsPrincipal).Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>